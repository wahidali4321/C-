#include<iostream>
using namespace std;
int main()
{
	int a,b,c;
	cout<<"enter the first number ? ";
	cin>>a;
	cout<<"enter the second number ? ";
	cin>>b;
	cout<<"enter the third number ? ";
	cin>>c;
	if(a>b)
	if(a>c)
	cout<<"the 1st value is greater than ";
	else
	cout<<"the 2nd  value is greater than ";
	if(b>c)
	cout<<"3rd number is greater  ";
	return 0;
}