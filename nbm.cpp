#include<iostream>
using namespace std;
int main()
{
	int a,b,c;
	cout<<"enter first no ? ";
	cin>>a;
	cout<<"enter second number ? ";
	cin>>b;
	cout<<" enter third number ? ";
	cin>>c;
	if(a>b)
	if(a>c)
	cout<<"1st number is greater ";
	else
	cout<<"3rd number is greater ";
	if(b>c)
	cout<<"2nd number is greater ";
	else
	cout<<"3rd numeber is greater ";
	return 9; 
}