#include<iostream>
using namespace std;
int main()
{
	int a,b,c;
	cout<<"enter the first number ";
	cin>>a;
	cout<<"enter the second number ? ";
	cin>>b;
	cout<<"enter the third number ? ";
	cin>>c;
	if(a>b)
	if(a>c)
	cout<<"the 1st value is greater than all ";
	else
	cout<<"the third value is greater ";
	if(b>c)
	cout<<"the 2nd value is greter than all ";
	else 
	cout<<"the third value is greate than all ";
	system("pause>0");
	return 0;
}