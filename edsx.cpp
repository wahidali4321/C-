#include<iostream>
using namespace std;
int main()
{
	int n;
	cout<<"enter the number that you want to check wheter the number is greater than 100 or not ? "<<endl;
	cin>>n;
	if(n>100)
	cout<<"the number is greater than 100 ? "<<endl;
	else
	cout<<"the number is less than 100 ? "<<endl;
	return 0;
}