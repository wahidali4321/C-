#include<iostream>
#include<conio.h>
#include<stdio.h>
#include<string.h>
using namespace std;
int main()
{
	char str[100];
	puts("enter the length of data ? ");
	gets(str);
	cout<<"length of string ? "<<strlen(str);
}